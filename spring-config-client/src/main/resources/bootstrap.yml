management:
  endpoints:
    web:
      exposure:
        include: '*'
  security:
    enabled: 'false'
spring:
  application:
    name: spring-config-server
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: production